# Sources

This directory is for sources that benefit from their own catalog description,
such as an archive, manual series, oral history, source tree, or museum
collection. A system concept may also cite an external source directly in its
`sources` frontmatter.

# Adding a Source

Create `sources/<source-slug>.md` from
[`templates/source.md.template`](../templates/source.md.template).
